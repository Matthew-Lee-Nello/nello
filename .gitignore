node_modules/
dist/
.next/
.turbo/
.env
.env.local
*.log
*.pid
store/
workspace/
.DS_Store
coverage/
.vercel/
tsconfig.tsbuildinfo
.vercel
.env*.local

# Local install artefacts — never commit (contain API keys + user-specific paths)
bundle.json
nello-bundle.json
.mcp.json
claude_desktop_config.json
brain-context.md
.claude/
vault/
.wa-session/
.serena/
# Rendered-from-template files for local install runs (the .hbs is the source)
/CLAUDE.md
/AGENTS.md
install.log
# Client override layer + rollback snapshots (per-install state; the self-update
# engine reads the overlay and writes rollback backups - never part of the product).
client-overlay/
.nello-rollback/
.nello-version
